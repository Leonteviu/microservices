version: '3.3'
services:
  mongo_db:
    image: mongo:3.2
    volumes:
      - post_db:/data/db
    networks:
      back_net:
        aliases:
          - post_db
          - comment_db
  ui:
    build: ./ui
    image: ${USERNAME}/ui:${UI_VERSION}
    ports:
      - ${EXTERNAL_PORT}:${CONTAINER_PORT}/${PROTOCOL}
    networks:
      - front_net
  post:
    build: ./post-py
    image: ${USERNAME}/post:${POST_VERSION}
    networks:
      back_net:
        aliases:
          - post_db
      front_net:
        aliases:
          - post_db
  comment:
    build: ./ui
    image: ${USERNAME}/comment:${COMMENT_VERSION}
    networks:
      back_net:
        aliases:
          - comment_db
      front_net:
        aliases:
          - comment_db
volumes:
  post_db:

networks:
  front_net:
    driver: bridge
    ipam:
      config:
      - subnet: ${FRONT_SUBNET}
  back_net:
    driver: bridge
    ipam:
      config:
      - subnet: ${BACK_SUBNET}
